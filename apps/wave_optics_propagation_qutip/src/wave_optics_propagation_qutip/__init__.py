"""Lens experiments simulated with QuTiP: the phase protocol as operators on kets.

The experiment, its simulation loop and its analysis are those of `python_wave_optics`;
this package provides the QuTiP backend, `backend.QutipBackend`.
"""
