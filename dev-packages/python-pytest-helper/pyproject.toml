[project]
name = "python-pytest-helper"
version = "0.1.0"
description = "Floating-point comparisons and hypothesis strategies of numbers, axes and signals for the unit tests of this monorepo."
readme = "README.md"
authors = [
    { name = "Siavash Davani", email = "siavash.davani@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "hypothesis>=6.140.3",
    "numpy>=2.2.6",
    "python-signals",
]

[build-system]
requires = ["uv_build>=0.8.18,<0.9.0"]
build-backend = "uv_build"

[tool.uv.sources]
python-signals = { workspace = true }
