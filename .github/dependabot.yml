# Quarterly updates of the GitHub Actions and of the locked Python dependencies.
version: 2
updates:
  - package-ecosystem: github-actions
    directories:
      - /
      - /.github/actions/*
    schedule:
      interval: quarterly
    groups:
      actions:
        patterns: ["*"]

  - package-ecosystem: uv
    directory: /
    schedule:
      interval: quarterly
    groups:
      python:
        patterns: ["*"]
