"""Evolutions under time-independent potentials and kinetic energies.

`direct` applies quadratic ones exactly, `sample_based` arbitrary ones of one sign with
the sample-based phase propagator, and `fourier` holds the transforms between the
position and the momentum domain.
"""
