// For format details, see https://aka.ms/devcontainer.json.
{
  "name": "Python",
  // Use the Dockerfile. More info: https://containers.dev/guide/dockerfile
  "build": {
    // Path is relative to the devcontainer.json file.
    "dockerfile": "Dockerfile"
  },
  // install the workspace with the documentation tools, and the pre-commit hooks
  "postCreateCommand": "uv sync --all-packages --group docs && uvx pre-commit install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "EditorConfig.EditorConfig",
        "charliermarsh.ruff",
        "tamasfe.even-better-toml",
        "github.vscode-github-actions",
        "ms-azuretools.vscode-docker",
        "streetsidesoftware.code-spell-checker",
        "esbenp.prettier-vscode",
        "Gruntfuggly.todo-tree",
        "ms-toolsai.jupyter",
        "redhat.vscode-yaml"
      ]
    }
  }
}
