# Publishes qiu-qiskit-aer-encore to PyPI when it changes on master; see .github/actions/publish-package
name: Publish qiu-qiskit-aer-encore

on:
  push:
    branches:
      - master
    paths:
      - "packages/qiu-qiskit-aer-encore/**"
  workflow_dispatch:

jobs:
  publish:
    name: "Test, build and publish qiu-qiskit-aer-encore"
    runs-on: ubuntu-latest
    environment: pypi

    permissions:
      contents: read
      id-token: write # to publish to PyPI as a trusted publisher, without tokens

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Publish
        uses: ./.github/actions/publish-package
        with:
          package: qiu-qiskit-aer-encore
          path: packages/qiu-qiskit-aer-encore
