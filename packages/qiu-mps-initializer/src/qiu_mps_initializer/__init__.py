"""State preparation with layers of gates from matrix product states (MPS).

`state_preparation.mps_state_preparation` prepares a state approximately with a given
number of layers of one- and two-qubit gates; `mps` provides the layers themselves.
"""
